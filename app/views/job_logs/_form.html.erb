<%= form_for(@job_log) do |f| %>
  <% if @job_log.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@job_log.errors.count, "error") %> prohibited this job_log from being saved:</h2>

      <ul>
      <% @job_log.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
